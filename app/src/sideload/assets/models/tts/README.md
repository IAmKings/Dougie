Place VITS TTS files here for the sideload APK (not committed):

- `model.onnx`
- `tokens.txt`
- `lexicon.txt`

Optional `dict/` is not required for seed. Weights are gitignored (`*.onnx`). Do not put `model.gguf` here.
