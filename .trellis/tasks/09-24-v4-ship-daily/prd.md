# V4 交到已发布的包里

## Goal

让已经在 `master` 上验收过的日常能力出现在下一次 GitHub Release 里，并让「打开某个应用」对不上允许列表时直接告诉用户，不再交给最小档模型。

## Already on master, not in v0.1.5

- 设置「常驻规则」（默认关）
- 定时「晨间简报」（只填草稿，不自动发送）
- 设置「隐私」
- 开发者页最小档工具契约。真机 `n=14 correct=12 passed=false`，两条「打开应用」没有调出 `app_intent`。不再改提示词，不换模型。

## Child map

| 目录 | 交付 | 顺序 |
|---|---|---|
| `09-24-release-0-1-6` | 版本号 0.1.6，README 与 Release 说明对齐，不新增行为 | 先做 |
| `09-24-open-app-miss` | 「打开」加一个不在允许列表里的别名时，用中文说明去添加，不进模型 | 发版之后 |

父任务不直接改代码。

## Out of scope

商店表格与截图、规则 D 的 500 条、Kokoro、自动读通知、端侧视觉、桌面端、新工具、再改最小档协议例句。
