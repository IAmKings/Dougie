# Implement: 版本 0.1.5 发版

## Order

1. `app/build.gradle.kts`：默认 `0.1.5` / 105。
2. README 两处 v0.1.4 / 104 → v0.1.5 / 105。
3. 提交。`git tag -a v0.1.5 -m "Dougie 0.1.5"`。push `HEAD` 与 tag。

## Verify

远程 tag `v0.1.5`；Actions 出现 Release run。
