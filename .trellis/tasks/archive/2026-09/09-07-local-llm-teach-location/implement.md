# Implement

1. `ChatPromptAssembler` teach 名单 + 定位示例文案。
2. 更新 `ChatPromptAssemblerTest` / `LocalToolCallParserTest`。
3. Spec：`directory-structure.md` 本地四件工具。
4. `./gradlew :core:llm:test :app:checkChannelLeak`
