# Implement: Phase 5m

1. `CharacterErrorRate` + tests.
2. `IntentEval` accuracy over parsed gold vs hypothesis JSON.
3. Tiny JSON fixtures in test resources (≥10 distinct gold intents in schema/list).
4. gitignore `eval/` for future wav dumps.
5. `./gradlew :core:tool:test` JDK 17.

Do not add sherpa/llama JNI to this test path.
