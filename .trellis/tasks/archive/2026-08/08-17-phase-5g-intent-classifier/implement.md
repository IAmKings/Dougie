# Implement: Phase 5g Intent Classifier

1. `IntentPort` / `IntentClassifierTool` + JVM tests.
2. `AndroidIntentPort` + wire in `DougieApplication`.
3. `*.gguf` gitignore; spec error/logging/directory.
4. JDK 17: `./gradlew :core:tool:test :app:checkChannelLeak`.
