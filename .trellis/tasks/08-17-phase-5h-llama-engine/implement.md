# Implement: Phase 5h Llama Engine

1. `IntentJsonParser` / `LlamaIntentEngine` + JVM tests.
2. `LlamaJni` loadLibrary gate; wire `AndroidIntentPort`.
3. Spec: directory-structure (no GGUF/NDK in git).
4. JDK 17: `./gradlew :core:tool:test :app:checkChannelLeak`.
