[permissions]
allow = ["Bash=cd /Users/pauldeman/Documents/ai_workspace/Dougie && echo \"== 版本 ==\" && grep -n \"V2.1.2\" PRD.md | head -2 && echo \"== FGA 引用 ==\" && grep -c 'FGA' PRD.md && echo \"== 三个新 Tool ==\" && for t in ScreenCaptureTool ScreenMatchTool TapSwipeTool; do echo -n \"$t: \"; grep -c \"$t\" PRD.md; done && echo \"== 决策 #15/#16 ==\" && grep -n \"决策 #15\\|决策 #16\" PRD.md | head -6 && echo \"== 代码块配对 ==\" && grep -c '^```' PRD.md && echo \"== 章节结构 ==\" && grep -n \"^# \\|^## 6.7\" PRD.md && echo \"== 表格分隔行 ==\" && grep -c '^|---' PRD.md && echo \"== 风险矩阵行数 ==\" && sed -n '/# 18. 风险矩阵/,/# 19\\./p' PRD.md | grep -c '^|'", "Bash=cd /Users/pauldeman/Documents/ai_workspace/Dougie && python3 - <<'EOF'\nimport re\nlines = open('PRD.md', encoding='utf-8').read().split('\\n')\ntables = []\ni = 0\nwhile i < len(lines):\n    if re.match(r'^\\s*\\|', lines[i]) and i+1 < len(lines) and re.match(r'^\\s*\\|[\\s\\-:|]+\\|\\s*$', lines[i+1]):\n        header_cols = lines[i].count('|') - 1\n        sep_cols = lines[i+1].count('|') - 1\n        j = i + 2\n        row_issues = []\n        while j < len(lines) and re.match(r'^\\s*\\|', lines[j]):\n            cols = lines[j].count('|') - 1\n            if cols != header_cols:\n                row_issues.append((j+1, cols))\n            j += 1\n        tables.append((i+1, header_cols, sep_cols, row_issues, j - i))\n        i = j\n    else:\n        i += 1\nprint(f\"表格总数: {len(tables)}\")\nbad = [t for t in tables if t[1] != t[2] or t[3]]\nfor t in tables:\n    status = \"OK \" if t[1] == t[2] and not t[3] else \"BAD\"\n    print(f\"{status} 表起始行{t[0]} 表头列数{t[1]} 分隔列数{t[2]} 行数{t[4]} 异常行:{t[3]}\")\nprint(\"异常表数量:\", len(bad))\nEOF", "Bash=cd /Users/pauldeman/Documents/ai_workspace/Dougie && python3 - <<'EOF'\nimport re\nlines = open('PRD.md', encoding='utf-8').read().split('\\n')\ntables, i = [], 0\nwhile i < len(lines):\n    if re.match(r'^\\s*\\|', lines[i]) and i+1 < len(lines) and re.match(r'^\\s*\\|[\\s\\-:|]+\\|\\s*$', lines[i+1]):\n        header_cols = lines[i].count('|') - 1\n        sep_cols = lines[i+1].count('|') - 1\n        j, issues = i + 2, []\n        while j < len(lines) and re.match(r'^\\s*\\|', lines[j]):\n            if lines[j].count('|') - 1 != header_cols:\n                issues.append(j+1)\n            j += 1\n        tables.append((i+1, header_cols, sep_cols, issues))\n        i = j\n    else:\n        i += 1\nbad = [t for t in tables if t[1] != t[2] or t[3]]\nprint(f\"表格总数: {len(tables)}, 异常: {len(bad)}\")\nfor t in tables:\n    if t[1] != t[2] or t[3]:\n        print(\"BAD:\", t)\nprint(\"OK\" if not bad else \"FAIL\")\nEOF", "Bash=cd /Users/pauldeman/Documents/ai_workspace/Dougie && echo \"== 版本 ==\" && grep -n \"V2.1.5\" PRD.md | head -1 && echo \"== 决策 #19 引用 ==\" && grep -c \"决策 #19\" PRD.md && echo \"== ASR 锚点 ==\" && grep -c \"sherpa-onnx\\|Paraformer-zh\\|SenseVoiceSmall\\|SpeechInputTool\" PRD.md && echo \"== §6.8 ==\" && grep -n \"^## 6.8\" PRD.md && echo \"== 结构 ==\" && grep -c '^```' PRD.md && grep -c '^|---' PRD.md && echo \"== 风险矩阵行数 ==\" && sed -n '/# 18. 风险矩阵/,/# 19\\./p' PRD.md | grep -c '^|' && echo \"== 表格列一致性 ==\" && python3 - <<'EOF'\nimport re\nlines = open('PRD.md', encoding='utf-8').read().split('\\n')\ntables, i = [], 0\nwhile i < len(lines):\n    if re.match(r'^\\s*\\|', lines[i]) and i+1 < len(lines) and re.match(r'^\\s*\\|[\\s\\-:|]+\\|\\s*$', lines[i+1]):\n        header_cols = lines[i].count('|') - 1\n        sep_cols = lines[i+1].count('|') - 1\n        j, issues = i + 2, []\n        while j < len(lines) and re.match(r'^\\s*\\|', lines[j]):\n            if lines[j].count('|') - 1 != header_cols:\n                issues.append(j+1)\n            j += 1\n        tables.append((i+1, header_cols, sep_cols, issues))\n        i = j\n    else:\n        i += 1\nbad = [t for t in tables if t[1] != t[2] or t[3]]\nprint(f\"表格总数: {len(tables)}, 异常: {len(bad)}\")\nprint(\"OK\" if not bad else f\"BAD: {bad}\")\nEOF", "Bash=cd /Users/pauldeman/Documents/ai_workspace/Dougie && grep -c \"决策 #20\\|vits-zh-hf-fanchen-C\\|Kokoro\\|SpeechOutputTool\\|系统原生\\|TextToSpeech\" PRD.md && grep -c '^```' PRD.md && grep -c '^|---' PRD.md && sed -n '/# 18. 风险矩阵/,/# 19\\./p' PRD.md | grep -c '^|' && grep -n \"V2.1.6\" PRD.md | head -1 && grep -n \"^## 6.9\\|^## 6.8\" PRD.md && grep -n \"13 项评审修正 + 7\" PRD.md && python3 - <<'EOF'\nimport re\nlines = open('PRD.md', encoding='utf-8').read().split('\\n')\ntables, i = [], 0\nwhile i < len(lines):\n    if re.match(r'^\\s*\\|', lines[i]) and i+1 < len(lines) and re.match(r'^\\s*\\|[\\s\\-:|]+\\|\\s*$', lines[i+1]):\n        header_cols = lines[i].count('|') - 1\n        sep_cols = lines[i+1].count('|') - 1\n        j, issues = i + 2, []\n        while j < len(lines) and re.match(r'^\\s*\\|', lines[j]):\n            if lines[j].count('|') - 1 != header_cols:\n                issues.append(j+1)\n            j += 1\n        tables.append((i+1, header_cols, sep_cols, issues))\n        i = j\n    else:\n        i += 1\nbad = [t for t in tables if t[1] != t[2] or t[3]]\nprint(f\"表格总数: {len(tables)}, 异常: {len(bad)}\")\nprint(\"OK\" if not bad else f\"BAD: {bad}\")\nEOF", "Bash=cd /Users/pauldeman/Documents/ai_workspace/Dougie && grep -c \"规则 A\\|规则 B\\|规则 C\\|遗留待定处理规则\" PRD.md && grep -c '^```' PRD.md && grep -c '^|---' PRD.md && grep -n \"V2.1.7\" PRD.md | head -1 && grep -n \"400MB\\|RTF ≤ 1.0\\|不得静默降级\" PRD.md && sed -n '/# 18. 风险矩阵/,/# 19\\./p' PRD.md | grep -c '^|' && python3 - <<'EOF'\nimport re\nlines = open('PRD.md', encoding='utf-8').read().split('\\n')\ntables, i = [], 0\nwhile i < len(lines):\n    if re.match(r'^\\s*\\|', lines[i]) and i+1 < len(lines) and re.match(r'^\\s*\\|[\\s\\-:|]+\\|\\s*$', lines[i+1]):\n        header_cols = lines[i].count('|') - 1\n        sep_cols = lines[i+1].count('|') - 1\n        j, issues = i + 2, []\n        while j < len(lines) and re.match(r'^\\s*\\|', lines[j]):\n            if lines[j].count('|') - 1 != header_cols:\n                issues.append(j+1)\n            j += 1\n        tables.append((i+1, header_cols, sep_cols, issues))\n        i = j\n    else:\n        i += 1\nbad = [t for t in tables if t[1] != t[2] or t[3]]\nprint(f\"表格总数: {len(tables)}, 异常: {len(bad)}\")\nprint(\"OK\" if not bad else f\"BAD: {bad}\")\nEOF", "Bash=cd /Users/pauldeman/Documents/ai_workspace/Dougie && grep -c \"### Case\" PRD.md && grep -c \"规则 D\" PRD.md && grep -c '^```' PRD.md && grep -c '^|---' PRD.md && grep -n \"V2.1.8\" PRD.md | head -1 && sed -n '/# 18. 风险矩阵/,/# 19\\./p' PRD.md | grep -c '^|' && python3 - <<'EOF'\nimport re\nlines = open('PRD.md', encoding='utf-8').read().split('\\n')\ntables, i = [], 0\nwhile i < len(lines):\n    if re.match(r'^\\s*\\|', lines[i]) and i+1 < len(lines) and re.match(r'^\\s*\\|[\\s\\-:|]+\\|\\s*$', lines[i+1]):\n        header_cols = lines[i].count('|') - 1\n        sep_cols = lines[i+1].count('|') - 1\n        j, issues = i + 2, []\n        while j < len(lines) and re.match(r'^\\s*\\|', lines[j]):\n            if lines[j].count('|') - 1 != header_cols:\n                issues.append(j+1)\n            j += 1\n        tables.append((i+1, header_cols, sep_cols, issues))\n        i = j\n    else:\n        i += 1\nbad = [t for t in tables if t[1] != t[2] or t[3]]\nprint(f\"表格总数: {len(tables)}, 异常: {len(bad)}\")\nprint(\"OK\" if not bad else f\"BAD: {bad}\")\nEOF", "Bash=cd /Users/pauldeman/Documents/ai_workspace/Dougie && python3 - <<'EOF'\nimport re, sys\ntext = open('PRD.md', encoding='utf-8').read()\nlines = text.split('\\n')\nok = True\ndef check(cond, msg):\n    global ok\n    print((\"PASS\" if cond else \"FAIL\") + \" - \" + msg)\n    if not cond: ok = False\n\ncheck(text.count('### Case') == 12, 'E2E Case 数量 = 12')\ncheck(text.count('规则 D') >= 2, '规则 D 落地 >= 2 处')\ncheck(text.count('```') % 2 == 0, '代码块配对为偶数')\ncheck(text.count('|---') == 24, '表格分隔行 = 24')\ncheck('V2.1.8' in text, '版本 V2.1.8 生效')\n# 表格列一致性\ntables, i = [], 0\nwhile i < len(lines):\n    if re.match(r'^\\s*\\|', lines[i]) and i+1 < len(lines) and re.match(r'^\\s*\\|[\\s\\-:|]+\\|\\s*$', lines[i+1]):\n        header_cols = lines[i].count('|') - 1\n        sep_cols = lines[i+1].count('|') - 1\n        j, issues = i + 2, []\n        while j < len(lines) and re.match(r'^\\s*\\|', lines[j]):\n            if lines[j].count('|') - 1 != header_cols:\n                issues.append(j+1)\n            j += 1\n        tables.append((i+1, header_cols == sep_cols and not issues))\n        i = j\n    else:\n        i += 1\ncheck(len(tables) == 24, '表格总数 = 24')\ncheck(all(t[1] for t in tables), '全部表格列一致(0 异常)')\nsys.exit(0 if ok else 1)\nEOF", "Bash=cd /Users/pauldeman/Documents/ai_workspace/Dougie && python3 - <<'EOF'\nimport re, sys\ntext = open('PRD.md', encoding='utf-8').read()\nlines = text.split('\\n')\nok = True\ndef check(cond, msg):\n    global ok\n    print((\"PASS\" if cond else \"FAIL\") + \" - \" + msg)\n    if not cond: ok = False\n\ncheck(text.count('### Case') == 12, 'E2E Case 数量 = 12')\ncheck(text.count('规则 D') >= 2, '规则 D 落地 >= 2 处')\ncheck(text.count('```') % 2 == 0, '代码块配对为偶数')\nsep_rows = sum(1 for l in lines if l.startswith('|---'))\ncheck(sep_rows == 24, f'表格分隔行 = 24 (实际 {sep_rows})')\ncheck('V2.1.8' in text, '版本 V2.1.8 生效')\ntables, i = [], 0\nwhile i < len(lines):\n    if re.match(r'^\\s*\\|', lines[i]) and i+1 < len(lines) and re.match(r'^\\s*\\|[\\s\\-:|]+\\|\\s*$', lines[i+1]):\n        header_cols = lines[i].count('|') - 1\n        sep_cols = lines[i+1].count('|') - 1\n        j, issues = i + 2, []\n        while j < len(lines) and re.match(r'^\\s*\\|', lines[j]):\n            if lines[j].count('|') - 1 != header_cols:\n                issues.append(j+1)\n            j += 1\n        tables.append((i+1, header_cols == sep_cols and not issues))\n        i = j\n    else:\n        i += 1\ncheck(len(tables) == 24, f'表格总数 = 24 (实际 {len(tables)})')\ncheck(all(t[1] for t in tables), '全部表格列一致(0 异常)')\nsys.exit(0 if ok else 1)\nEOF"]
