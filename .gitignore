# ── macOS ─────────────────────────────────────────────
.DS_Store

# ── Python ────────────────────────────────────────────
__pycache__/
*.pyc

# ── IDE / 编辑器 ──────────────────────────────────────
.idea/
.vscode/
*.iml

# ── Android / Gradle 构建产物（未来工程） ─────────────
build/
.gradle/
.kotlin/
local.properties
*.apk
*.aab
*.onnx
!core/tool/src/test/resources/intent-pack/model.onnx
!feature/settings/src/test/resources/intent-pack/model.onnx
*.gguf
**/.cxx/
# Sideload may drop weights in app/src/sideload/assets/models/{asr,tts}/ (layout READMEs stay).
**/jniLibs/
third_party/llama.cpp/

# Optional full ASR dumps (wav); never commit 500-clip sets or models.
/eval/

# ── 环境变量与密钥（绝不入库） ────────────────────────
.env
.env.*
*.jks
*.keystore
!*.env.example

# ── 本地运行时状态（会话级，随会话重建） ──────────────
.reasonix/*.json
.reasonix/*.log
